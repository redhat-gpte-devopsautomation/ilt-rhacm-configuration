---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: acm-configuration
  namespace: acm-configuration
spec:
  clusterSelector:
    matchLabels:
      local-cluster: 'true'
